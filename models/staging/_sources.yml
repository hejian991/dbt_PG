version: 2

sources:
  - name: public
    description: "Public schema containing raw data tables"
    tables:
      - name: raw_customers
        description: "Raw customer data from seeds"
        columns:
          - name: id
            data_type: integer
            description: "Customer ID - primary key"
          - name: first_name
            data_type: text
            description: "Customer first name"
          - name: last_name
            data_type: text
            description: "Customer last name"

